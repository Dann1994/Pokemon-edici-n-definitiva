-- Sharing metadata (25-community-and-ecosystem.md 3.2).  Read by tooling
-- and the manager detail pane; never by the loader's merge.
return {
  summary = "Modern phys/spec split, FAIRY/STEEL/DARK types, opt-in bug-free ruleset.",
  author = "pokered-plus",
  contact = "https://github.com/bryanthaboi/gen1recomp",
  tags = { "modernisation", "mechanic", "types", "balance" },
  differences = {
    changed = {
      "every damaging move carries its Gen 4+ physical/special category; damage uses it instead of the Gen 1 type-based split",
      "the type chart is the full Gen 6 chart (Ghost/Psychic bug fixed, Bug<->Poison nerfed, Fire resists Ice, etc.)",
      "CLEFAIRY, CLEFABLE -> FAIRY; JIGGLYPUFF, WIGGLYTUFF -> NORMAL/FAIRY; MR_MIME -> PSYCHIC/FAIRY; MAGNEMITE, MAGNETON -> ELECTRIC/STEEL",
      "'modern' (bug-free Gen 1 battle) is the default ruleset -- see src/battle/rulesets/modern.lua and the stock-defaults branch",
    },
    added = {
      "FAIRY, STEEL and DARK types",
    },
    known = {
      "species retypes are the canonical Gen 6 typings",
      "Yellow battle sprites are applied by scripts/pokered_plus_yellow_gfx.lua (a cache overlay), not by this mod",
    },
  },
  credits = {
    { who = "PokeAPI", for_ = "the Gen 4+ move damage_class reference" },
    { who = "pret/pokered", for_ = "the base move and species tables this patches over" },
  },
  compat = { engine = ">=0.0.0-0 <2.0.0", modApi = 2 },
}
